%%%%%%%% ICML 2025 — Explicit vs Implicit World Models %%%%%%%%%%%%%%%%%%%%%%%%
\documentclass{article}

\usepackage{microtype}
\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{enumitem}

\newcommand{\theHalgorithm}{\arabic{algorithm}}

\usepackage[accepted]{icml2025}
\icmltitlerunning{Explicit vs Implicit World Models for Decision Making}

\begin{document}

\twocolumn[
\icmltitle{Explicit vs Implicit World Models for Decision Making:\\
           A Comparison of Generative and Task-Centric Latent Representations}

\begin{icmlauthorlist}
\icmlauthor{Daniel Sakharov}{mipt}
\end{icmlauthorlist}

\icmlaffiliation{mipt}{Moscow Institute of Physics and Technology (MIPT), Dolgoprudny, Russia}
\icmlcorrespondingauthor{Daniel Sakharov}{sakharov.da@phystech.edu}

\icmlkeywords{model-based reinforcement learning, world models, latent representations,
              representation learning, manipulation, locomotion}

\vskip 0.3in
]

\begin{abstract}
Model-based reinforcement learning agents differ fundamentally in the objective used
to shape their latent representation: generative agents reconstruct observations, while
task-centric agents optimise consistency and temporal-difference losses.
This work implements and compares a DreamerV3-style Recurrent State-Space Model (RSSM)
and a TD-MPC2-style implicit world model across three environments spanning manipulation
and locomotion (ManiSkill2 \texttt{LiftCube-v0}, DMControl \texttt{cartpole\_swingup},
and \texttt{walker\_walk}), with 3 seeds each, and characterises their latent spaces via
snapshot-based linear probing, geometry evolution, KL utilisation analysis,
MPPI planner health monitoring, and Canonical Correlation Analysis (CCA).

Contrary to the hypothesis that reconstruction pressure yields more linearly decodable
representations, the implicit agent achieves higher linear probing $R^2$ in all three
environments (0.87--0.97 vs.\ 0.47--0.76), while also substantially outperforming the
explicit agent in task return (2--38$\times$ higher, averaged over 3 seeds).
The mechanistic explanation is \emph{KL utilisation collapse} in the explicit agent:
the number of active discrete categorical variables drops from 7 (out of 8) to below 1
between training steps 25k--40k, after which the world model loses the capacity to
represent distinct states, and task performance does not recover.
Geometry analysis reveals that explicit and implicit latent spaces evolve in opposite
directions: the explicit participation ratio decreases as variables collapse, while the
implicit participation ratio grows as the encoder refines its world representation.
CCA shows only moderate cross-agent alignment (mean $\approx 0.51$) throughout training,
indicating each agent discovers a distinct representational basis despite operating on
identical observations.
\end{abstract}

%------------------------------------------------------------------------------
\section{Introduction}
\label{sec:intro}
%------------------------------------------------------------------------------

Model-based reinforcement learning (MBRL) agents learn a compressed world model to
support planning and policy optimisation without requiring a simulator at inference
time~\citep{hafner2020dreamer,hansen2022tdmpc}.
Two broad representational families have emerged with fundamentally different inductive
biases regarding what the latent space must encode.

\textbf{Explicit world models}~\citep{hafner2021dreamerv2,hafner2023dreamerv3} learn a
generative model that can reconstruct observations.
The latent state must preserve enough information to regenerate what was seen, creating
direct gradient pressure to encode all observation dimensions.
Policy learning occurs entirely in the imagined latent space via actor-critic
methods~\citep{schulman2015gae}.

\textbf{Implicit world models}~\citep{hansen2022tdmpc,hansen2024tdmpc2} forgo
reconstruction entirely.
The latent is trained via a consistency loss (predicted next-state should match the
encoder's output) and temporal-difference (TD) losses on rewards and Q-values.
At decision time, planning is performed in the latent space via
MPPI~\citep{williams2017mppi}.

The central question examined here is:
\textit{Does generative reconstruction pressure produce latent representations that are
more linearly decodable in terms of physical state coordinates, and does this translate
to better control performance?}

Both families are implemented in a unified training harness, evaluated across three
benchmark environments (3 seeds each), and characterised via six complementary analyses
designed to reveal structure invisible in performance curves alone.
Against the original hypothesis, the results show that the implicit agent achieves
higher task performance \emph{and} higher linear probing $R^2$ across all environments.
KL utilisation collapse is identified as the mechanistic failure mode of the explicit
agent, with continuous training-time evidence provided via 20 latent snapshots per run.

%------------------------------------------------------------------------------
\section{Background}
\label{sec:background}
%------------------------------------------------------------------------------

\textbf{The foundational vision.}
\citet{ha2018worldmodels} demonstrated that an agent can learn a compressed spatial
and temporal representation of its environment in an unsupervised manner, building a
``mental model'' that enables future trajectory simulation.
This established that a complex environment can be distilled into a compact
representation, making downstream policy learning significantly more efficient.

\textbf{Explicit world models.}
The Dreamer series~\citep{hafner2020dreamer,hafner2021dreamerv2,hafner2023dreamerv3}
uses the Recurrent State-Space Model (RSSM) to learn an environment model that predicts
future observations, rewards, and terminal signals.
DreamerV3 extended this into a general-purpose algorithm with fixed hyperparameters.
The discrete categorical latent with Unimix regularisation forces all variables to
carry information, controlled by a KL divergence loss with free-bits.

\textbf{Implicit world models.}
TD-MPC2~\citep{hansen2024tdmpc2} shifts focus from reconstruction to prediction of
task-relevant information.
Rather than imagining pixel-perfect futures, it uses TD learning and MPC to shape a
latent space guided by value-driven objectives, achieving high sample efficiency across
continuous-control tasks.
SimNorm activations~\citep{hansen2024tdmpc2} project each group of 8 latent units onto
the probability simplex, providing a structural guard against representational collapse.

\textbf{The central tension.}
Explicit models offer a rich internal world that may benefit complex planning, but risk
wasting capacity on task-irrelevant details.
Implicit models encode only what is necessary to maximise value but may miss structure
that could support longer-horizon reasoning.
The experiments presented here quantify these trade-offs and track how each
representation evolves during training.

%------------------------------------------------------------------------------
\section{Agents and Architecture}
\label{sec:agents}
%------------------------------------------------------------------------------

\subsection{Explicit Agent (RSSM / DreamerV3-style)}
\label{sec:explicit}

The explicit agent learns a Recurrent State-Space Model (RSSM)~\citep{hafner2019rssm}
whose latent state comprises two parts.

\textbf{Deterministic state} $h_t \in \mathbb{R}^{256}$ is a GRU hidden state:
\begin{equation}
  h_t = \mathrm{GRU}(h_{t-1},\,[z_{t-1};\, a_{t-1}]).
\end{equation}

\textbf{Stochastic state} $z_t$ is sampled from a discrete categorical posterior with
$8$ variables $\times$ $8$ bins, yielding a $64$-dimensional one-hot vector:
\begin{equation}
  z_t \sim \mathrm{Cat}\!\left(\mathrm{MLP}(h_t,\, \mathrm{embed}_t)\right).
\end{equation}

The full feature vector fed to the policy is $[h_t;\, z_t^{\mathrm{flat}}] \in \mathbb{R}^{320}$.
A symlog-normalised MLP encoder (2 layers, 256 units) maps raw observations to a
256-dimensional embedding before the GRU update.
Latent snapshots for analysis use $z_t$ only (the $64$-dim stochastic component),
since $h_t$ is initialised from a zeroed state during batch encoding and conveys no
per-observation information in that context.

The world model is trained jointly with three prediction heads:

\begin{table}[h]
\caption{Explicit agent prediction heads.}
\label{tab:heads}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lll}
\toprule
Head        & Output                      & Loss \\
\midrule
Decoder     & Reconstructed obs           & Symlog MSE \\
Reward      & 255-bin symlog-discrete     & Cross-entropy \\
Continuation & Bernoulli ($1-$done)       & Binary CE \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

KL divergence between learned posterior and prior is minimised with free bits
($\mathrm{kl\_free}=1.0$, $\lambda_{\mathrm{dyn}}=0.5$, $\lambda_{\mathrm{rep}}=0.1$).
An \textbf{actor-critic} (\textit{ImagBehavior}) is trained entirely in imagination:
the actor rolls out $H=10$ steps in latent space and a $\lambda$-return critic
($\lambda=0.95$, $\gamma=0.997$) with a slow EMA target provides value estimates.

\subsection{Implicit Agent (TD-MPC2-style)}
\label{sec:implicit}

The implicit agent learns a $64$-dimensional continuous latent space without any
decoder~\citep{hansen2024tdmpc2}.
The encoder uses SimNorm activations (simplicial normalisation that maps each group of
8 latent units onto the probability simplex):
\begin{equation}
  z_t = \mathrm{SimNorm}\!\left(\mathrm{MLP}(\mathrm{symlog}(\mathrm{obs}_t))\right)
  \in \mathbb{R}^{64}.
\end{equation}

A latent dynamics model predicts $z_{t+1} = \mathrm{SimNorm}(\mathrm{MLP}([z_t; a_t]))$.
A 2-ensemble Q-network maps $(z_t, a_t)$ to 101-bin two-hot encoded Q-values.

\textbf{Training losses} (batch: 256 transitions, horizon $H=5$):

\begin{table}[h]
\caption{Implicit agent training objectives.}
\label{tab:implicit-losses}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lll}
\toprule
Loss        & Description                                      & Weight \\
\midrule
Consistency & $\|\mathrm{dyn}(z_t,a_t) - \mathrm{sg}(z_{t+1})\|^2$ & 20.0 \\
Reward      & Soft cross-entropy (two-hot)                     & 0.1 \\
Value       & TD loss (two-hot, min-Q target)                  & 0.1 \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Action selection uses MPPI/CEM planning~\citep{williams2017mppi,rubinstein1999cem}:
4 CEM iterations $\times$ 64 sampled trajectories $\times$ $H=5$ horizon in latent
space, warm-started from the previous step's plan.

\subsection{Shared Infrastructure and Environments}

Both agents share a unified replay buffer (200k transitions), symlog observation
normalisation (applied inside each encoder), and a checkpoint manager that retains
evenly-spaced snapshots for temporal analysis.
Additionally, every 10k steps, 512 transitions are sampled from the buffer and each
agent's encoder is applied under \texttt{torch.no\_grad()}, saving \texttt{(obs, z,
reward, action)} as a compressed NPZ file (latent snapshots, $\approx$200 KB each,
20 per run).
This provides 20 temporal data points for geometry and probing analysis without the
cost of retaining full checkpoints.

Experiments are conducted on three environments:
\begin{itemize}[nosep,leftmargin=1em]
  \item \textbf{ManiSkill2 LiftCube-v0} \citep{gu2023maniskill2}: 42-dim state,
        8-dim action, precise tabletop manipulation with dense reward, 200 steps/episode.
  \item \textbf{DMControl cartpole\_swingup} \citep{tunyasuvunakool2020dmcontrol}: 5-dim state,
        1-dim action, classic balance task from the DMControl suite.
  \item \textbf{DMControl walker\_walk} \citep{tunyasuvunakool2020dmcontrol}: 24-dim state,
        6-dim action, bipedal locomotion requiring coordinated gait.
\end{itemize}
All runs use 200k environment steps and 3 random seeds.

%------------------------------------------------------------------------------
\section{Experimental Comparison}
\label{sec:experiments}
%------------------------------------------------------------------------------

\subsection{Learning Curves and Performance}

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/learning_curves}}
\caption{Learning curves on \texttt{LiftCube-v0} (200k steps, 3 seeds, shaded
         $\pm 1$ std).
         The explicit agent's return collapses after step 40k; the implicit agent
         achieves higher but noisier performance.}
\label{fig:learning_curves}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../figures_v2/asymptotic_performance}}
\caption{Asymptotic performance (mean return over final 50k steps, 3 seeds).
         The implicit agent dominates across all three environments.}
\label{fig:performance_summary}
\end{center}
\vskip -0.2in
\end{figure}

\begin{table}[t]
\caption{Asymptotic performance (final 50k steps, mean over 3 seeds).}
\label{tab:performance}
\vskip 0.15in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lcc}
\toprule
Environment              & Explicit & Implicit \\
\midrule
LiftCube-v0 (ManiSkill) & 9.1      & \textbf{19.9} \\
cartpole\_swingup (DMC) & 91.9     & \textbf{828.6} \\
walker\_walk (DMC)      & 25.4     & \textbf{959.4} \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Figure~\ref{fig:learning_curves} and Table~\ref{tab:performance} show that the implicit
agent substantially outperforms the explicit agent in all three environments.
The performance gap is largest on the locomotion tasks (38$\times$ for walker\_walk),
where the explicit agent's world model must also reconstruct high-dimensional joint
state vectors, increasing the reconstruction overhead.
On the manipulation task (LiftCube-v0), the gap is smaller (2.2$\times$) — both agents
struggle with the precise contact dynamics, but the implicit agent sustains a learning
signal throughout.

\subsection{KL Utilisation Collapse: The Explicit Agent's Failure Mode}

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/kl_utilization}}
\caption{KL utilisation over training on \texttt{LiftCube-v0} (3 seeds).
         Top: \texttt{kl\_active\_vars} = number of categorical variables with KL$>$0.1
         nats (out of 8 total). Bottom: \texttt{kl\_min\_var} = minimum per-variable KL.
         Both collapse after step 25k, coinciding with performance collapse in
         Figure~\ref{fig:learning_curves}.}
\label{fig:kl_collapse}
\end{center}
\vskip -0.2in
\end{figure}

The explicit agent exhibits a consistent and catastrophic \emph{KL utilisation collapse}
across all three seeds of LiftCube-v0 (Figure~\ref{fig:kl_collapse}).
During training steps 1k--25k, the number of active categorical variables
(\texttt{kl\_active\_vars}, defined as variables with KL $> 0.1$ nats) rises from
2.4 to 7.0 out of 8, indicating that most discrete factors are being used to encode
state.
However, between steps 25k and 38k, \texttt{kl\_active\_vars} collapses from 7.0 to
below 0.5 and does not recover.

This collapse has a direct mechanistic interpretation: as fewer categorical variables
carry KL divergence, the posterior $q(z_t | h_t, o_t)$ approaches the prior
$p(z_t | h_t)$ for most variables.
The agent loses the information-theoretic pressure to encode distinct observations,
and the stochastic component of the latent becomes near-degenerate.
Crucially, this collapse coincides exactly with the performance collapse visible in
Figure~\ref{fig:learning_curves}: the explicit agent's return, which had risen to
$\approx 30$ at step 40k, falls to below 10 and remains there.

The collapse is likely driven by the free-bits mechanism interacting with the limited
update frequency (\texttt{train\_ratio}$=64$ means 3{,}125 world model updates over
200k steps).
When the world model has not yet learned to predict diverse future states, the KL loss
drives the posterior toward the prior (lower KL = lower loss), and once this degenerate
solution is reached, the reconstruction loss alone is insufficient to push the posterior
back toward expressiveness.
The DMControl environments show similar patterns in the KL utilisation plots (see
Appendix~\ref{app:per_env}), confirming that this failure mode is not
environment-specific.

\textbf{Implicit agent health.}
The implicit agent does not suffer from an equivalent collapse.
The SimNorm constraint prevents representational degeneration, and the consistency loss
maintains a stable one-step prediction error (\texttt{consistency\_t0}$\approx 1.0
\times 10^{-4}$) throughout training.
The horizon degradation ratio \texttt{consistency\_tH}/\texttt{consistency\_t0} drops
from 1.01 (step 10k) to $\approx 0.64$ (step 60k), meaning that multi-step predictions
converge faster than single-step predictions — a sign of a healthy dynamics model rather
than horizon collapse.
MPPI planner diversity (elite-value std) remains volatile but positive throughout,
indicating the planner continues to explore distinct action sequences.

%------------------------------------------------------------------------------
\section{Latent Space Analysis}
\label{sec:analysis}
%------------------------------------------------------------------------------

\subsection{Linear Probing}

A Ridge regression ($\alpha=1.0$, 5-fold CV) was trained at each of 20 latent
snapshots to predict each observation coordinate from the frozen latent vector,
yielding a continuous curve of median $R^2$ over training.
Results at the final snapshot (step 200k) are reported in Table~\ref{tab:probing}.

\begin{table}[h]
\caption{Linear probing median $R^2$ at step 200k (3-seed average).}
\label{tab:probing}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lcc}
\toprule
Environment              & Explicit & Implicit \\
\midrule
LiftCube-v0 (ManiSkill) & 0.604    & \textbf{0.927} \\
cartpole\_swingup (DMC) & 0.757    & \textbf{0.971} \\
walker\_walk (DMC)      & 0.466    & \textbf{0.877} \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

\textbf{The implicit agent achieves higher linear probing $R^2$ in all three
environments}, contrary to the hypothesis that reconstruction pressure would produce
more linearly decodable representations.
The explanation lies in the KL collapse: once the explicit agent's categorical variables
degenerate, the stochastic latent $z_t$ carries less information about the current
observation, reducing the regression's ability to decode physical coordinates.
The implicit agent, constrained by SimNorm and trained with a consistency loss that
forces the encoder to be approximately invertible by the dynamics model, maintains a
continuous and structured encoding of state.

Figure~\ref{fig:r2_evolution} shows the R² evolution over 20 snapshot steps for
LiftCube-v0.
The explicit agent's R² peaks around step 30k (coinciding with peak KL utilisation,
before collapse) and declines thereafter.
The implicit agent's R² remains high and stable throughout.

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/r2_evolution}}
\caption{Linear probing median $R^2$ over 20 snapshot steps on \texttt{LiftCube-v0}
         (3 seeds, shaded $\pm 1$ std).
         The explicit agent's R² peaks before the KL collapse and declines with it;
         the implicit agent remains stable.}
\label{fig:r2_evolution}
\end{center}
\vskip -0.2in
\end{figure}

\subsection{Latent Geometry Evolution}

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/geometry_evolution}}
\caption{Latent geometry over 20 snapshot steps on \texttt{LiftCube-v0} (3 seeds).
         Top: participation ratio (PR = $(\sum\lambda)^2 / \sum\lambda^2$).
         Bottom: number of principal components needed for 95\% variance.
         Explicit PR decreases (variables collapse); implicit PR grows (representation
         spreads across more directions).}
\label{fig:geometry}
\end{center}
\vskip -0.2in
\end{figure}

The participation ratio (PR) and effective dimensionality (dims$_{95\%}$) are computed
from the empirical covariance of 512 latent vectors at each snapshot step.
The participation ratio is defined as $\mathrm{PR} = (\sum_i \lambda_i)^2 / \sum_i
\lambda_i^2$, where $\lambda_i$ are the eigenvalues; it equals the latent dimension
when all eigenvalues are equal.

For ManiSkill LiftCube-v0 (Figure~\ref{fig:geometry}):
\begin{itemize}[nosep,leftmargin=1em]
  \item \textbf{Explicit} (64-dim stochastic z): PR decreases from 46.1 at step 10k to
        $\approx 33$--$38$ by step 200k; dims$_{95\%}$ decreases from 51 to 48--50.
  \item \textbf{Implicit} (64-dim continuous): PR grows from 9.9 at step 10k to
        $\approx 13.9$ by step 60k; dims$_{95\%}$ grows from 20 to 26--28.
\end{itemize}

The two agents evolve in \emph{opposite directions}.
The explicit agent starts with a high-participation-ratio latent (due to the categorical
prior encouraging uniformity across bins) that gradually loses effective dimensions as
KL collapse reduces the number of active variables.
The implicit agent starts concentrated in a few directions (driven by the dominant
consistency signal on a few task-relevant axes) and gradually spreads as it discovers
more structure in the environment.
Neither agent produces dead units (dimensions with std~$< 0.01$) throughout training,
confirming that the KL collapse is about \emph{inter-variable collapse}, not individual
dimension death.

\subsection{Reward Predictability}
\label{sec:reward_probe}

To understand why the implicit agent's latent is more useful for control despite
encoding fewer observation details (lower nominal PR), a linear probe is applied to
assess how well each representation linearises the reward landscape (single-seed
analysis on LiftCube-v0).

\begin{table}[h]
\caption{Reward predictability probe ($R^2$, LiftCube-v0, seed 1).}
\label{tab:reward}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lcc}
\toprule
Feature             & Explicit  & Implicit \\
\midrule
Observation only    & 0.983     & $-0.910$ \\
Latent only         & \textbf{0.995} & \textbf{0.842} \\
Gain (latent$-$obs) & $+0.012$  & $+1.752$ \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

For the \textbf{explicit agent}, raw observations already predict reward with $R^2 =
0.983$; the latent adds only $0.012$ over that baseline.
The explicit latent is a near-faithful compressed copy of the observation, and in this
environment the observation is nearly sufficient for reward prediction.

For the \textbf{implicit agent}, raw observations achieve $R^2 = -0.91$: the
relationship between observation coordinates and reward is sufficiently nonlinear that
even the mean prediction outperforms a linear fit.
The latent achieves $R^2 = 0.842$, gaining $+1.752$ over the observation baseline.
The implicit encoder has \emph{linearised the reward landscape}: its latent space is a
coordinate system in which the reward function is approximately linear, even though the
raw observation space is not.

This explains the performance gap: the implicit agent's consistency and TD losses
jointly produce a representation in which both planning and policy learning operate on
a reward-predictable manifold.
The explicit agent, by contrast, learns a representation optimised to reconstruct
observations — a task in which reward linearity is not directly incentivised.

\subsection{CCA Alignment}

CCA was applied at each shared snapshot step to latent vectors produced by both agents
on the same 512 buffer observations.

\begin{table}[h]
\caption{Cross-agent CCA at step 200k (3-seed average).}
\label{tab:cca}
\vskip 0.1in
\begin{center}
\begin{small}
\begin{sc}
\begin{tabular}{lcc}
\toprule
Environment              & Mean CCA & Top-1 CCA \\
\midrule
LiftCube-v0 (ManiSkill) & 0.515    & 0.626 \\
cartpole\_swingup (DMC) & 0.492    & 0.598 \\
walker\_walk (DMC)      & 0.509    & 0.598 \\
\bottomrule
\end{tabular}
\end{sc}
\end{small}
\end{center}
\vskip -0.1in
\end{table}

Mean canonical correlations of $\approx 0.50$ indicate only moderate linear alignment
between the two latent spaces — substantially lower than the near-perfect correlation
($> 0.97$) often found between representations learned on identical data with similar
architectures~\citep{kornblith2019similarity}.
The top canonical dimension reaches only 0.60--0.63, suggesting that even the
best-shared axis is not perfectly aligned.

Figure~\ref{fig:cca_timeline} shows that CCA alignment is stable throughout training
($\pm 0.02$ across all steps), which indicates that both agents \emph{lock in} their
representational bases early and do not converge toward each other as training
proceeds.
This is consistent with the geometry analysis: the two agents follow distinct
evolutionary trajectories (one collapsing, one expanding) in their respective 64-dim
latent spaces, with only moderate overlap in what they choose to encode.

\begin{figure}[t]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/cca_timeline}}
\caption{Cross-agent CCA over 20 snapshot steps on \texttt{LiftCube-v0} (3 seeds).
         Alignment stabilises at $\approx 0.50$ early in training and does not
         converge despite both agents observing identical environment data.}
\label{fig:cca_timeline}
\end{center}
\vskip -0.2in
\end{figure}

%------------------------------------------------------------------------------
\section{Summary and Hypothesis Assessment}
\label{sec:summary}
%------------------------------------------------------------------------------

This work set out to test whether generative reconstruction pressure produces more
linearly decodable latents that translate to better control.

\textbf{Not supported:} Higher observation-decodability does not accompany
reconstruction pressure in the conducted experiments.
The implicit agent achieves higher linear probing $R^2$ in all three environments
(0.87--0.97 vs.\ 0.47--0.76).
This reversal of the expected direction is explained by KL utilisation collapse in
the explicit agent: once the discrete categorical variables cease to carry divergence,
the stochastic latent loses its ability to encode distinct observations, reducing linear
probing performance.

\textbf{Main finding — KL collapse:} The explicit agent's failure mode is not
underfitting or insufficient planning capacity, but a representational degeneration
event.
The number of active categorical variables drops from 7 to below 1 between steps 25k
and 38k on LiftCube-v0, with a concurrent and permanent collapse in task performance.
The free-bits mechanism~\citep{hafner2023dreamerv3} is intended to prevent this by
tolerating some rate of posterior-prior collapse, but at this update frequency it
instead creates a stable attractor at the degenerate solution.
Addressing this collapse — through adaptive free-bits, higher update frequency, or
an auxiliary KL floor — is the most direct path to improving the explicit agent.

\textbf{Geometry divergence:} Explicit and implicit latent spaces evolve in opposite
directions.
The explicit PR decreases (46→33) as variables collapse; the implicit PR grows
(9.9→13.9) as the encoder progressively spreads information across more directions.
These opposite trajectories arise from opposite inductive biases: the explicit agent's
discrete uniform prior initially encourages high participation ratio, while the implicit
agent's consistency signal initially focuses on a few high-variance axes and broadens
as training stabilises.

\textbf{Representational independence:} The two agents discover distinct representational
bases (CCA $\approx 0.50$) despite observing identical environment trajectories.
This moderate alignment, stable from early in training, indicates that the choice of
world model objective has a lasting and fundamental effect on what information is
encoded in the latent space.

\textbf{Implicit agent limitation:} The implicit agent achieves higher returns but with
higher variance, particularly on LiftCube-v0.
The MPPI planner's elite-value diversity collapses at times, suggesting Q-value overfit.
Extending the horizon beyond 5 steps or adding a model-based rollout penalty could
stabilise planning.

%------------------------------------------------------------------------------
\section{Bonus: SmolVLA Vision Encoder vs.\ World Model Latents}
\label{sec:smolvla}
%------------------------------------------------------------------------------

To situate the two world-model representations in a broader context, SmolVLA
\citep{smolvla2025}—a 500M-parameter Vision-Language-Action model using a
SigLIP vision encoder (via SmolVLM-2)—is fine-tuned on LiftCube-v0 and its
visual representations are compared to the world-model latents.

\textbf{Setup.}
100 demonstration episodes are generated in ManiSkill2 and saved in LeRobot
format (Parquet + MP4).
SmolVLA is fine-tuned for 1\,000 gradient steps using LoRA
($r{=}16$, $\alpha{=}32$, targeting all attention and FFN projection layers),
without 4-bit quantisation (the 500M-parameter model occupies ${\approx}1$\,GB
at bfloat16).
For the comparison, 512 paired $(\text{state}_{42}, \text{RGB}_{224\times224\times3})$
observations are collected from the environment.
World-model latents $z \in \mathbb{R}^{64}$ are obtained via the agents'
\texttt{encode()} method.
SmolVLA visual tokens (64 tokens $\times$ 1152 dims per image) are mean-pooled
to $z_\text{VLA} \in \mathbb{R}^{1152}$, then reduced to 50 PCA dimensions
before CCA (required since 512 samples $\ll$ 1152 features).

\textbf{Results.}

\begin{table}[h]
\centering
\small
\caption{Linear probing $R^2$ (median over 42 state coordinates), latent geometry,
         and pairwise CCA alignment on LiftCube-v0.}
\label{tab:smolvla}
\begin{tabular}{lrrrrr}
\toprule
Representation & $R^2$ & PR & dims$_{95}$ & dead & CCA$_\text{vs.\ implicit}$ \\
\midrule
Explicit (RSSM)     & $-1.53$ & 34.4 & 46  & 0.062 & 0.69 \\
Implicit (TD-MPC2)  & $+0.87$ &  4.4 &  7  & 0.016 & —    \\
SmolVLA (SigLIP)    & $-3.52$ & 353.8 & 407 & 0.000 & 0.49 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Linear probing.}
The implicit agent's latent retains most task-relevant state information
($R^2{=}0.87$), consistent with its consistency-based objective.
Both the explicit RSSM ($R^2{=}{-}1.53$) and SmolVLA ($R^2{=}{-}3.52$)
are not linearly decodable into raw joint coordinates:
the RSSM because its single-step discrete posterior is computed without
temporal GRU context, and SmolVLA because its SigLIP features encode visual
semantics (textures, shapes, scene layout) rather than metric robot state.
The negative values indicate that the linear probe predicts state coordinates
worse than the global mean—consistent with a representation space in which
the relevant structure is non-linear or absent.

\textbf{Latent geometry.}
The three representations occupy qualitatively different geometric regimes.
The implicit agent's latent is extremely compact (PR$\approx 4.4$, dims$_{95}{=}7$),
reflecting a task-focused, low-redundancy encoding enforced by SimNorm.
The explicit RSSM occupies moderate dimensionality (PR$\approx 34$, dims$_{95}{=}46$).
SmolVLA occupies a vastly larger effective space (PR$\approx 354$,
dims$_{95}{=}407$): the SigLIP encoder was pre-trained on diverse internet
imagery, so its 1152-dimensional space captures hundreds of orthogonal visual
axes unrelated to this specific manipulation task.
The zero dead-unit fraction in SmolVLA reflects this broad coverage.

\textbf{CCA alignment.}
The two world models share moderate linear structure with each other
(mean CCA $\approx 0.69$), expected since both encode the same 42-dimensional
state observations.
SmolVLA aligns weakly with both world models (mean CCA $\approx 0.49$–$0.50$),
close to the noise floor, confirming that the VLM and state-based world models
encode fundamentally different information despite operating on the same scenes.

\textbf{Takeaway.}
Fine-tuned SmolVLA's vision encoder and state-based world models are
complementary rather than interchangeable.
The world models capture compact, task-aligned state structure; SmolVLA captures
rich visual semantics distributed across hundreds of dimensions.
Combining both—for example, by conditioning planning on VLM-encoded language
goals while using a state-based world model for dynamics prediction—is a natural
direction for future work.

%------------------------------------------------------------------------------
\bibliography{paper}
\bibliographystyle{icml2025}
%------------------------------------------------------------------------------

%------------------------------------------------------------------------------
\appendix
\section{Per-Environment Detailed Plots}
\label{app:per_env}
%------------------------------------------------------------------------------

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_cartpole_swingup/learning_curves}}
\caption{Learning curves on \texttt{cartpole\_swingup} (3 seeds).
         The implicit agent dominates from step 10k onward;
         the explicit agent shows limited but stable improvement.}
\label{fig:lc_cartpole}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_walker_walk/learning_curves}}
\caption{Learning curves on \texttt{walker\_walk} (3 seeds).
         The implicit agent achieves near-expert locomotion ($\approx 950$ return);
         the explicit agent remains well below 100.}
\label{fig:lc_walker}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_cartpole_swingup/kl_utilization}}
\caption{KL utilisation on \texttt{cartpole\_swingup} (3 seeds).
         A similar collapse pattern as LiftCube-v0 is observed, with recovery
         partially occurring in some seeds.}
\label{fig:kl_cartpole}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_walker_walk/kl_utilization}}
\caption{KL utilisation on \texttt{walker\_walk} (3 seeds).
         The collapse is consistent; the explicit agent never recovers full variable
         utilisation on the locomotion task.}
\label{fig:kl_walker}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/maniskill_LiftCube-v0/planner_health}}
\caption{MPPI planner health on \texttt{LiftCube-v0} (3 seeds).
         Top: one-step consistency error (\texttt{consistency\_t0}).
         Middle: multi-step consistency error (\texttt{consistency\_tH}).
         Bottom: elite-value diversity (\texttt{mppi\_elite\_value\_std}).
         The horizon degradation ratio tH/t0 drops below 1 after step 10k (healthy
         dynamics), and planner diversity remains volatile but non-zero.}
\label{fig:planner_health}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../figures_v2/sample_efficiency}}
\caption{Sample efficiency: steps to reach 50\% of each agent's peak return across
         all three environments.
         The implicit agent reaches criterion faster on all tasks.}
\label{fig:sample_efficiency}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_cartpole_swingup/geometry_evolution}}
\caption{Geometry evolution on \texttt{cartpole\_swingup} (3 seeds).
         The same opposing trends (explicit PR decreasing, implicit PR growing)
         are observed as on LiftCube-v0.}
\label{fig:geo_cartpole}
\end{center}
\vskip -0.2in
\end{figure}

\begin{figure}[h]
\vskip 0.2in
\begin{center}
\centerline{\includegraphics[width=\columnwidth]{../analysis_v2/aggregated/dmc_walker_walk/geometry_evolution}}
\caption{Geometry evolution on \texttt{walker\_walk} (3 seeds).
         The explicit agent's participation ratio decreases more sharply on this
         higher-dimensional observation task.}
\label{fig:geo_walker}
\end{center}
\vskip -0.2in
\end{figure}

\end{document}
